<p id="notice"><%= notice %></p>

<p>
  <strong>Image URL: <%= @photo.image_url %></strong>
</p>
<%= image_tag @photo.image_url, alt: @photo.comment%>
<p>
<%= @photo.comment %>
</p>

<%= link_to 'Edit', edit_photo_path(@photo) %> |
<%= link_to 'Back', photos_path %>
